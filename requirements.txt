# Core
streamlit>=1.30.0
openai>=1.12.0
python-dotenv>=1.0.0

# PDF processing — input side (Apache 2.0)
pypdfium2>=4.20.0
Pillow>=10.0.0

# Reports are now HTML (browser-renderable). For PDF, the user can Ctrl+P
# in the browser → "Save as PDF". This eliminates the previous PyMuPDF/AGPL
# dependency entirely. See LICENSE_REVIEW.md.

# OCR fallback
pytesseract>=0.3.10

# Data / export
pandas>=2.0.0
openpyxl>=3.1.0

# Testing
pytest>=7.4.0
